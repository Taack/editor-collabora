plugins {
    id 'taack-grails-plugin'
    id 'taack-grails-assets-common'
}

grails {
    // should use gradle -Dgrails.run.active=true bootRun
    exploded = true
    plugins {
        implementation project(':crew')
    }
}
